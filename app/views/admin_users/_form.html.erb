<% #partial template can save time whem we have repetitive stuff in different templates %>

 
<%# we created a helper method for rendering partial form, so this method could be further simplified %>

<%=error_messages_for(@admin_user) %>

<%# error_messages_for is a method written in helper folder %>
		<table summary="Admin User form fields">
			<tr>
				<th><%=f.label(:first_name, "First name")%></th> 
				<td><%=f.text_field(:first_name) %></td>
			</tr>
			
			<tr>
				<th><%=f.label(:last_name, "Last name")%></th> 
				<td><%=f.text_field(:last_name) %></td>
			</tr>
			
			<tr>
				<th><%=f.label(:username)%></th> 
				<td><%=f.text_field(:username) %></td>
			</tr>
			
			<tr>
				<th><%=f.label(:email)%></th> 
				<td><%=f.text_field(:email) %></td>
			</tr>
			
			<tr>
				<th><%=f.label(:email_confirmation)%></th> 
				<td><%=f.text_field(:email_confirmation) %></td>
			</tr>
			
		
			<tr>
				<th><%=f.label(:password)%></th> 
				<td><%=f.password_field(:password) %></td>
			</tr>
		
			
		</table>		